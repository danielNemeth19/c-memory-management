#ifndef ENUMS_H
#define ENUMS_H

typedef enum Color_old {
    RED_OLD,
    GREEN_OLD,
    BLUE_OLD
} color_old_t;

typedef enum Color {
    RED = 55,
    GREEN = 176,
    BLUE = 38
} color_t;

#endif

